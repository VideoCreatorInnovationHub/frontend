import { ComponentPreloadingStrategy } from './component-preloading-strategy';

describe('ComponentPreloadingStrategy', () => {
  it('should create an instance', () => {
    expect(new ComponentPreloadingStrategy()).toBeTruthy();
  });
});
